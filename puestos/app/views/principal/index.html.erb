
<div class="container">
	<div class="centrado">
		<i class="material-icons cuenta">account_circle</i><br>
		<%if user_signed_in? %>
			<%= current_user.email%><br/>
			<%if false%>
				<%= link_to 'Destroy', user_registration_path, :confirm => 'Are you sure you want to destroy your account?', :method => :delete %>
			<%end%>
		<%end%>
	</div>
</div>

